Stat unit: big figure, optional drawn proportion, uppercase label, handwritten aside.

```jsx
<StatBlock figure="12" label="Rooms this month" note="two more booked">
  <TallyCount count={12} color="violet" height={30}/>
</StatBlock>
```

Use  Children are optional — a figure that has no honest denominator gets none, and that is fine.
