Round icon-only button for toolbars, dismissals, and compact actions. Always pass `label`.

```jsx
<IconButton label="Close"><svg …/></IconButton>
<IconButton label="Share" variant="secondary" size="lg"><svg …/></IconButton>
```

Variants `ghost|secondary|primary`; sizes `sm|md|lg`. Pass a Lucide SVG as the child.
