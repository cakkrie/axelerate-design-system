Status pill — short, 1–2 words, never a sentence.

```jsx
<Badge>Member</Badge>
<Badge tone="volt">Most loved</Badge>
<Badge tone="success">Live</Badge>
```

Tones: `brand` (default) · `volt` (hype) · `success|warning|danger` · `neutral`.
