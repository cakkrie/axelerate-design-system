Radio with inline label — one-of choices. Share a `name` across the group.

```jsx
<Radio name="pace" label="Monthly" defaultChecked />
<Radio name="pace" label="Yearly (2 months free)" />
```

Depends on Checkbox's `.ax-choice` label styles (same directory).
