Checkbox with inline label — multi-select choices and confirmations.

```jsx
<Checkbox label="Email me when a room opens" defaultChecked />
<Checkbox label="I agree to the member code" />
```

Native checkbox props pass through (`checked`, `onChange`, `defaultChecked`).
